#include<stdio.h>
#include<conio.h>
main(){
 int vetor[8], i, num;
printf("Digite 8 numeros:");
for (i=0 ; i<8; i++)
scanf ("%d",&vetor[i]);

getch();
printf("%d%d%d%d%d%d%d%d",vetor[7], vetor[6], vetor[5], vetor[4], vetor[0], vetor[1], vetor[2], vetor[3]);
}
